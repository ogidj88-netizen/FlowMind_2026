# MIGRATION ROADMAP V1

Status: FROZEN HISTORICAL DOCUMENT  
Branch: `cashflow-mode`

## Meaning

This document is kept only as historical context from the earlier dual-system / migration phase.

It is **not** the active strategy for the current repository cleanup.

## Current truth

The current active direction is:

- do not expand migration as a working track
- do not build around compatibility bridges
- do not maintain dual control-layer thinking
- clean the repository toward one active control contour

## Why this file is frozen

This roadmap belongs to an earlier decision model where:

- legacy runtime and canonical dispatcher were treated as separate systems
- staged migration was considered the main path
- bridge/mapping/decommission planning was an active concern

That is no longer the active operating frame for Phase 2.

## What this file may still be used for

This file may still be referenced only as:

- historical architecture context
- explanation of previous migration thinking
- evidence of why hybrid runtime logic was considered dangerous

## What this file must NOT be used for

Do not use this file as:

- current execution roadmap
- active cleanup plan
- active architecture strategy
- justification for reintroducing migration work as the main track

## Active Phase 2 rule

The repository is currently being cleaned toward:

- one active control brain
- one active command surface
- one active state model
- removal of legacy control ambiguity

## Final note

Migration is not the active workstream now.

Repository cleanup and single-contour stabilization are the active workstream.
