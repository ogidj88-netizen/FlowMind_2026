# READ-ONLY COMPATIBILITY BRIDGE

Status: FROZEN HISTORICAL DOCUMENT  
Branch: `cashflow-mode`

## Meaning

This document is preserved only as historical context from the earlier compatibility-bridge / migration phase.

It is **not** an active Phase 2 implementation plan.

## Current truth

The repository is not currently expanding compatibility-bridge work as an active track.

The active direction is:
- repository cleanup
- single-contour stabilization
- removal of legacy control ambiguity
- one active control brain
- one active command surface
- one active state model

## Why this document is frozen

This document belongs to an earlier decision frame where:

- read-only bridge design was treated as an active migration concern
- legacy runtime compatibility boundaries were being formalized
- M3-style bridge preparation was considered part of the working plan

That is no longer the active operating frame.

## What this file may still be used for

This file may still be referenced only as:

- historical architecture context
- explanation of previous compatibility thinking
- evidence that dual-write and writable bridge designs were explicitly rejected

## What this file must NOT be used for

Do not use this file as:

- active cleanup plan
- active compatibility roadmap
- active bridge implementation guide
- justification for restarting migration work as the main track

## Final note

If compatibility tooling remains in the repository, it is frozen, narrow, and non-authoritative.

It is not part of the active canonical control contour.
